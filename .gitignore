# Later drops still being reviewed. Not part of the megafork tree.
_new/

# Local / machine junk
.DS_Store
**/.DS_Store
*.log

# Repo-local tracker and handoff state — stay on disk, not in git
.memento/
.handoff/

# presence package build
presence/node_modules/
presence/dist/
presence/dist-demo/

# Global `~/.gitignore` has `_*/`, which would skip `__tests__/`.
!presence/src/__tests__/
!presence/src/__tests__/**
