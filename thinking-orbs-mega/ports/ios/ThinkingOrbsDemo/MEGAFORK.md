Vendored into `thinking-orbs-mega` from
`thinking-orbs-swift-netseye-ThinkingOrbsDemo-main.zip`.

Independent Swift / SwiftUI port of the original nine states. Open
`ThinkingOrbsDemo.xcodeproj`. The TypeScript package at the repo root is
the feature-complete surface; this app is the iOS host binding.
