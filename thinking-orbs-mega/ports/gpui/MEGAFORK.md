Vendored into `thinking-orbs-mega/ports/gpui` from
`gpui-thinking-orbs-master.zip` (Franco Escobar).

The TypeScript package at the repo root is the feature-complete surface
(original 9 + extras + this crate's focus / gyro / echo states ported back
as `focusing`, `pondering`, `recalling`). This crate is the native GPUI
widget.
